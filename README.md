# Public Commons

Local help, events, and discovery — Flutter Web + Firebase.

## Firebase / Firestore

The Dart package name is **public_commons** (imports use `package:public_commons/...`). This repo is wired to the **`public-commons`** Firebase project via `lib/firebase_options.dart`. To point at another project, run `flutterfire configure`.
