/// Viewports below this width use a compact, app-style layout (bottom navigation).
/// At or above this width, use a wider, site-style layout (navigation rail + centered content).
const double kWideLayoutBreakpoint = 900;

/// Cover image height for posts and events when the viewport is at or above [kWideLayoutBreakpoint].
const double kLargeLayoutPostImageHeight = 320;
